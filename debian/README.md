# Debian Packaging for AIC8800D80 Linux Driver

This repository contains the Debian packaging for the AIC8800D80 Linux driver, providing the necessary debian/ directory and packaging files to build .deb packages for the driver and firmware.

TODO:
- GitHub Actions generate .deb packages on push / release events
- Change package names to differ-from / conflict-with `radxa-aic8800`
- Remove `Makefile` and replace with documentation (it's just a few commands)
- Testing
- General cleanup
